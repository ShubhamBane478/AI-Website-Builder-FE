# Set USE_PROXY=true if you are NOT running the backend locally.
# API calls will be proxied through Vite to the Railway backend.
# Set USE_PROXY=false (default) if you ARE running the backend locally.
USE_PROXY=false
VITE_API_BASE_URL=http://localhost:8080
